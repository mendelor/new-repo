node {
        if(env.BRANCH_NAME == 'master'){
			stage('Build Master') {
                echo 'Building master'
            }
        }
		
		if(env.BRANCH_NAME == 'worker'){
			stage('Build Dev') {            
                echo 'Building dev'
            }
        }
}
